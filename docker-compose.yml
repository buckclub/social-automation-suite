# Social Automation Suite (fork of reels-automation)
# Author: Faheem Alvi <faheemalvi2000@gmail.com>

services:
  social-automation-suite:
    build: .
    container_name: social-automation-suite
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./posts:/app/posts
      - ./videos:/app/videos
      - ./backgrounds:/app/backgrounds
      - ./audio:/app/audio
      - ./config.json:/app/config.json
      - ./channels.json:/app/channels.json
    environment:
      - PYTHONUNBUFFERED=1
